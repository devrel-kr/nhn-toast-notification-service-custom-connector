name: Pull Request

on:
  push:
    branches:
    - 'feature/*'
  pull_request:
    branches:    
    - main

jobs:
  call_CI_part:
    uses: ./.github/workflows/build-test.yaml
    with:
      action_name: pull_request
